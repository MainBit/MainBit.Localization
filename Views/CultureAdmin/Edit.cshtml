@model MainBit.Localization.Models.DomainLocalizationSettingsItemRecord
@using (Html.BeginFormAntiForgeryPost()) {
    @Html.ValidationSummary()
    @Html.HiddenFor(m => m.DomainLocalizationSettingsPartRecord_Id);
    <fieldset>
        <ul>
            <li>
                @Html.LabelFor(m => m.Culture)
                @Html.EditorFor(m => m.Culture)
                <span class="hint">@T("")</span>
            </li>
            <li>
                @Html.LabelFor(m => m.DisplayName)
                @Html.EditorFor(m => m.DisplayName)
                <span class="hint">@T("")</span>
            </li>
            <li>
                @Html.LabelFor(m => m.Domain)
                @Html.EditorFor(m => m.Domain)
                <span class="hint">@T("")</span>
            </li>
            <li>
                @Html.LabelFor(m => m.Position)
                @Html.EditorFor(m => m.Position)
                <span class="hint">@T("")</span>
            </li>
        </ul>
    </fieldset>
    <fieldset>
        <button class="primaryAction" name="submit" value="@T("Save")" type="submit">@T("Save")</button>
        @Html.Link(T("Delete").Text, Url.Action("Delete", "CultureAdmin", new { id = @Model.Id }), new {itemprop = "RemoveUrl UnsafeUrl"})
    </fieldset>
}